### Contributing a Script

1. Fork this repository
2. Add your script as `script-library/<your-script-name>.js`
3. Add a matching `script-library/<your-script-name>.json` with `title`, `description`, `tags`, and `created` fields
4. Open a pull request